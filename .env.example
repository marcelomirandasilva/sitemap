APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# ============================================================
# Integração com a API Python de Geração de Sitemaps
# ============================================================

# URL base da API Python (ajuste conforme o ambiente)
# Desenvolvimento local: http://127.0.0.1:8000
# Servidor de produção: http://207.180.232.129:8000 (ou domínio configurado)
SITEMAP_API_URL=http://127.0.0.1:8000

# Credenciais legadas (mantidas por retrocompatibilidade — preferir API Keys)
SITEMAP_API_USERNAME=admin
SITEMAP_API_PASSWORD=admin123
SITEMAP_API_TIMEOUT=30

# Caminho local opcional para os artefatos gerados pela API Python.
# Use quando Laravel e API estiverem no mesmo servidor, mas em pastas diferentes.
# Exemplo producao: /home/marcelo1/htdocs/testemarcelo.1208.pro/sitemaps
SITEMAP_ARTIFACTS_PATH=

# Token secreto compartilhado com a API Python para requisições server-to-server.
# Este token deve ser IDÊNTICO ao SITEMAP_INTERNAL_SECRET configurado no .env da API Python.
# Gere com: openssl rand -hex 32
SITEMAP_INTERNAL_SECRET=gere_um_token_aleatorio_com_openssl_rand_hex_32

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

APP_MAINTENANCE_DRIVER=file
# APP_MAINTENANCE_STORE=database

# PHP_CLI_SERVER_WORKERS=4

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=debug

DB_CONNECTION=sqlite
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=laravel
# DB_USERNAME=root
# DB_PASSWORD=

SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=null

BROADCAST_CONNECTION=reverb
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database

CACHE_STORE=database
# CACHE_PREFIX=

MEMCACHED_HOST=127.0.0.1

REDIS_CLIENT=phpredis
REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=log
MAIL_SCHEME=null
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_FROM_ADDRESS="no-reply@genmap.app"
MAIL_FROM_NAME="${APP_NAME}"
MAILGUN_DOMAIN=
MAILGUN_SECRET=
MAILGUN_ENDPOINT=api.mailgun.net
RESEND_API_KEY=

AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
AWS_BUCKET=
AWS_USE_PATH_STYLE_ENDPOINT=false

VITE_APP_NAME="${APP_NAME}"

REVERB_APP_ID=local-app
REVERB_APP_KEY=local-key
REVERB_APP_SECRET=local-secret
REVERB_HOST=localhost
REVERB_PORT=8080
REVERB_SCHEME=http

VITE_REVERB_APP_KEY="${REVERB_APP_KEY}"
VITE_REVERB_HOST="${REVERB_HOST}"
VITE_REVERB_PORT="${REVERB_PORT}"
VITE_REVERB_SCHEME="${REVERB_SCHEME}"
